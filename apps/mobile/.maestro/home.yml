appId: com.healthyapp.app
---
# A signed-out launch shows the splash for two seconds, then onboarding.
- launchApp
- assertVisible:
    id: 'splash-screen'
- extendedWaitUntil:
    visible:
      id: 'onboarding-advance'
    timeout: 10000
- assertVisible:
    id: 'onboarding-login'
